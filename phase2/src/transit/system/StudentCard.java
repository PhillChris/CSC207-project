package transit.system;

/** Represents a student card in this simulation*/
public class StudentCard extends Card {

  /**
   * Constructs a new student card in this simulation
   *
   * @param cardID the id of the card being created
   */
  public StudentCard(int cardID) {
    super(cardID);
  }
}
